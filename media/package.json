{
  "name": "cerberus-media",
  "version": "0.1.0",
  "private": true,
  "description": "Cerberus media gateway worker (R2 read-through cache over per-artist tunnels)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260628.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
